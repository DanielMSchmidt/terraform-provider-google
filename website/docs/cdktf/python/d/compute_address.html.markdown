---
subcategory: "Compute Engine"
description: |-
  Get the IP address from a static address.
---


<!-- Please do not edit this file, it is generated. -->
# google\_compute\_address

Get the IP address from a static address. For more information see
the official [API](https://cloud.google.com/compute/docs/reference/latest/addresses/get) documentation.

## Example Usage

```python
import constructs as constructs
import cdktf as cdktf
# Provider bindings are generated by running cdktf get.
# See https://cdk.tf/provider-generation for more details.
import ...gen.providers.google as google
class MyConvertedCode(cdktf.TerraformStack):
    def __init__(self, scope, name):
        super().__init__(scope, name)
        google_dns_managed_zone_prod = google.dns_managed_zone.DnsManagedZone(self, "prod",
            dns_name="prod.mydomain.com.",
            name="prod-zone"
        )
        data_google_compute_address_my_address =
        google.data_google_compute_address.DataGoogleComputeAddress(self, "my_address",
            name="foobar"
        )
        google.dns_record_set.DnsRecordSet(self, "frontend",
            managed_zone=cdktf.Token.as_string(google_dns_managed_zone_prod.name),
            name="frontend.${" + google_dns_managed_zone_prod.dns_name + "}",
            rrdatas=[
                cdktf.Token.as_string(data_google_compute_address_my_address.address)
            ],
            ttl=300,
            type="A"
        )
```

## Argument Reference

The following arguments are supported:

* `name` - (Required) A unique name for the resource, required by GCE.

- - -

* `project` - (Optional) The project in which the resource belongs. If it
    is not provided, the provider project is used.

* `region` - (Optional) The Region in which the created address reside.
    If it is not provided, the provider region is used.

## Attributes Reference

In addition to the arguments listed above, the following computed attributes are
exported:

* `self_link` - The URI of the created resource.
* `address` - The IP of the created resource.
* `status` - Indicates if the address is used. Possible values are: RESERVED or IN_USE.

<!-- cache-key: cdktf-0.17.0-pre.15 input-4f83ad670d132b2a1b630f9147354ef05bd91b11eef4e406de501eb598784d2d -->